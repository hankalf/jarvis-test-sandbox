fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.26
icalendar>=5.0
recurring-ical-events>=2.1
pydantic>=2.5
PyYAML>=6.0
tzdata>=2024.1
python-multipart>=0.0.9

# Photo normalising on upload: EXIF rotation, downscaling, and HEIC from
# iPhones. Without these the server still runs, but only accepts formats the
# browser can already display and stores them at full size.
Pillow>=10.2
pillow-heif>=0.16

# Only needed when importer.enabled is true (phase 2 -- extraction of
# appointments from email/texts via the Claude API):
# anthropic>=0.40

# Only needed for the matching presence backend; harmless to install anyway.
# pyserial>=3.5      # presence.backend: serial  (LD2410 mmWave over USB)
# gpiozero>=2.0      # presence.backend: gpio    (Raspberry Pi only)
# lgpio>=0.2         # gpiozero's pin driver on Pi 5
